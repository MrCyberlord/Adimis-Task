import { Button } from '@/components/ui/button'
import React from 'react'

const page = () => {
  return (
    <div>
      <Button variant={"blue"}>
        PushMe
      </Button>
    </div>
  )
}

export default page
